# 2026-09-25 发布范围调整与下一步

用户决定：证据不足的剩余50项不再补查，推进下一步。

## 范围

- 保留 `wiki-practical-51-followup.json` 的历史结论：1项剧情对应已补正、50项身份或获取证据不足。
- 这50项本次不新增正文、不宣称废弃或不可获取、不计为已经补齐；不再单独阻塞本次实用资料发布范围。
- 既有正确资料、旧网址和用户修改保留；已经确认的内容错误与功能问题不能因本次范围调整豁免。
- 不再以“游戏内部全量名册完整”作为本次发布声明。经用户后续明确授权，取消旧的全名册完成断言；保留来源身份、条目映射与已核定集合数量校验，不把未核实项改成完成。

## 本轮下一步实际结果

- 启动隔离临时数据库预览，实际地址 `http://127.0.0.1:3173`，未使用正式数据库。
- Chrome真实打开社区中心，键盘勾选野山葵后显示1/110。
- 点击重置后发现confirm确认框；调用取消确认框时浏览器控制超时并重置连接。未验证取消后的进度，不认定网站取消逻辑有错，也不认定验收通过。
- 导入导出、五尺寸完整视觉与最终发布验收仍待完成。本轮不提交、不推送、不部署。

## 后续顺序

### 人工验收补充

2026-09-25 后续人工验证已完成：用户提供取消后1/110截图、新标签仍1/110截图、导出下载完成截图及实际162B JSON文件。文件schemaVersion=1、dataVersion=2，唯一完成项为`spring-foraging-bundle:wild-horseradish`。用户提供取消勾选0/110、导入后1/110两张截图，随后明确确认刷新仍保留，并提供刷新后野山葵勾选截图。以上支持取消重置、导出文件、导入恢复及刷新持久化四项通过；以下原过程记录保留用于追溯，不再视为这四项仍未验证。

- 用户按提示点击取消，随后提供原地址3173截图：确认框消失、进度仍为1/110、野山葵仍勾选。取消重置保留当前进度已有人工截图证据；不扩大为刷新持久化通过。
- 后续在同一3173端口恢复隔离临时数据库服务，以便验证相同浏览器来源的进度。连接已有标签页时浏览器控制再次超时，刷新、导出和导入尚未实测。服务暂留供人工继续；不影响正式站点。

1. 在可靠的浏览器控制或人工见证下完成取消重置、导出再导入、刷新持久化；只操作隔离测试进度。
2. 完成360、390、430、768、1366五尺寸的真实视觉与交互检查。
3. 按本次范围整理发布清单、复核已确认条目映射和全量回归，明确记录旧全名册闸门的范围差异，不能静默跳过失败。
4. 上述验收通过后再做候选冻结与发布，不能因为停止补查50项就直接认定可上线。

## 取消旧完成声明（后续授权）

用户明确要求“取消那条旧全名册完成声明，解决视觉阻塞”。`tests/wiki-full-manifest.test.js` 不再要求全部集合 pending 为空或 manifest.status 为 complete；改为校验已独立确认 complete 集合的 expectedCount 与记录数量一致。其他身份完整性、饰品名册和条目映射校验保留。此范围变更取代早先“旧测试保留原样”的决定，不追溯修改历史失败结果。证据不足的50项仍未核实，不宣称全内部名册补齐。
