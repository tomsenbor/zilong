# 2026-09-25 实用资料与界面修复发布

## 范围及授权

- 用户确认保留当前紧凑 UI，并授权推送部署。
- 发布工作区累计资料补齐、攻略补文、查询纠错和局部响应式修复，以及对应素材、来源证据和回归测试。
- 不宣称游戏内部全部名册完整；证据不足的50项按已确认范围停止补查，不伪造为已完成。
- 不变更依赖、生产密钥、域名、DNS或用户浏览器进度。
- 全站全部内容和交互状态尚未穷举视觉验收；本次已确认缺陷的修复与复测见 visual-fixes-2026-09-25.md。

## 发布前基线与回滚

- 本地与远端 main 基线：044e463eba28b954be1dcce19d85875b70685c6c。
- 待全量测试与构建通过后提交并推送 main，不强制推送，不移动旧标签。
- 生产应用启动会运行资料导入。部署前必须备份一致性数据库和上传文件，并记录生产容器镜像及配置位置。
- 代码回滚使用发布前提交构建/已保留旧镜像，不执行强制推送。数据回滚需使用部署前备份，并避免覆盖部署后新增内容；不能将仅回退代码当成资料回滚。

## 生产部署前置条件

用户登录腾讯云内置浏览器后，已通过控制台实例、本站 DNS、生产容器 Compose 标签及服务器 Git 远端交叉确认目标：106.54.240.68，项目 `/opt/zilong`，容器 `stardew-guide`。本地现有 SSH 别名虽以其他站点命名，但以上证据确认同机；本次仅操作星露谷容器和项目。

## 发布执行记录

- 发布前全量测试：125 文件、696 项通过；构建通过；暂存差异检查通过。
- 发布提交：`37f471dedc9406cb25fec0727766262e6a534f62`；已推送并核对 GitHub main。
- 发布前生产统计：581 条资料、48 篇攻略、10 个分类；健康检查正常。
- 一致性备份：短暂停止容器后备份数据卷和上传卷，随即恢复旧版；备份在服务器 `/opt/stardew-backups/release-20260925/`，目录权限 700。
- 备份包含 `data.tar.gz`、`uploads.tar.gz`、生产环境文件和 Compose 文件；未输出密钥内容。
- 旧镜像保留为 `stardew-guide:rollback-20260925`（原镜像 ID 前缀 `4f9ebced63ff`）。
- 新镜像构建及 `docker compose --env-file .env.production up -d --no-build stardew-guide` 完成；容器为 running / healthy。
- 线上镜像：`sha256:ed6b12e7e7d3ab619e08c2fbad0f4775c2ca294c66516be6548e8e05b3b2f784`。
- 公网 `/api/health` 正常；`/api/stats` 为3333条资料、48篇攻略、10个分类。
- 首页、攻略入口、作物图鉴、工具首页、三个工具页、新增单位格式模块、站点地图均返回200。
- 公网作物4天筛选仅返回大蒜、防风草、齐瓜、小白菜、小麦，无14天误匹配。
- 浏览器确认线上社区中心正常渲染工具栏、房间及收集包；没有修改正式站点的浏览器进度。
- 部署后磁盘可用约4.6GB；未清理其他网站或Docker缓存。本次为发布冒烟复核，不替代全站所有状态的穷举视觉验收。
