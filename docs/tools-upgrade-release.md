# 实用工具升级发布状态

2026-09-26：已发布。实施分支 `codex/practical-tools-upgrade` 已快进合并到 `main`，基线 `3b8cc121`。

用户明确要求“跳过直接发布”，授权跳过尚未完成的人工交互验收。未验证范围及真实证据见 `tools-upgrade-acceptance.md`；豁免不等同验收通过。仍执行自动化测试、构建、生产备份和线上冒烟检查。

恢复顺序：

1. 真实文件导入导出、确认窗口及剩余交互验收按用户决定暂缓；不清空生产用户进度。
2. 复查最终差异和构建，记录最终提交标识；仅提交本次批准文件，排除临时库、导出进度与凭据。
3. 刷新远程分支与生产运行版本，确认腾讯云目标、项目目录、磁盘及备份；不得把历史连接记录当作当前确认。
4. 部署前记录旧镜像/提交、独立备份与回滚命令；只更新该站，不影响同服务器其他网站，不迁移数据库。
5. 推送部署后验证首页、物品百科、四工具及关键链接，记录线上响应和版本；失败恢复已记录的旧版本。

## 本次备份与回滚点

- 已重新核对腾讯云目标 `106.54.240.68`、`/opt/zilong`、服务 `stardew-guide`；仓库远端为 `tomsenbor/zilong`。
- 发布前生产代码 `3b8cc1216671280950a09e5c2f610e5df3623171`，容器镜像 `sha256:ed6b12e7e7d3ab619e08c2fbad0f4775c2ca294c66516be6548e8e05b3b2f784`。
- 发布前3333条资料、48篇攻略、10个分类；容器 healthy，磁盘可用约5GB。
- 一致性备份 `/opt/stardew-backups/release-20260926-tools-005326/`：数据库、上传卷、生产环境文件和Compose文件。备份目录700，配置文件600；压缩包目录校验通过。短暂停止本站容器完成备份后已恢复 healthy。
- 回滚镜像 `stardew-guide:rollback-20260926-tools-005326` 已保留。
- 代码回滚：将该回滚镜像重新标记为 `stardew-guide-stardew-guide:latest`，在 `/opt/zilong` 执行 `docker compose --env-file .env.production up -d --no-build stardew-guide`，随后复查健康与统计。不删除卷、不回滚其他站点、不强制覆盖Git工作区。
- 若需数据恢复，应先保留故障时的数据并确认新增写入，另行恢复一致性备份；代码回滚不会自动回滚数据。

## 发布完成证据

- 运行代码提交：`a27df28f5adbb85b006f5d9f08cf06e9f7170dd6`，GitHub main 推送和远端哈希核对成功。
- 本轮发布前全量测试：130文件、720项通过，459.62秒；`npm.cmd run build`、`git diff --cached --check`通过。没有lint脚本。构建仅有caniuse-lite过期提示；服务器有buildx缺失提示但使用现有builder构建成功。
- 服务器直连GitHub超时；中止本次挂起的拉取进程后，通过SSH传送Git增量包，双方验证bundle及目标提交哈希，再`git merge --ff-only FETCH_HEAD`。未更改代理、远端URL或生产网络配置。
- 服务器镜像构建成功，Compose仅重建`stardew-guide`。容器启动时间北京时间2026-09-26 01:07:12，状态running/healthy。
- 新镜像：`sha256:f4a65b015a4bb6b38aa4a3907ce5caa9d08997ac5831b9e91c25ef8d772a70ac`。
- 公网健康检查正常；3333条资料、48篇攻略、10个分类保持不变。部署后磁盘约4.8GB可用。
- 首页、工具首页、四工具、物品列表和代表详情、新增JS模块均HTTP200；礼物未知季节为400而不是500；站点地图包含新工具。
- 礼物接口34位村民、138种具体物品；文森特春10日及葡萄关系、紫水晶+春季仅艾米丽均断言通过。社区中心资料链接、鬼鱼资料链接正常。
- 正式浏览器实际显示文森特五种本期最爱、艾米丽物品查询结果以及四工具入口。没有修改正式社区中心勾选或浏览器存储。
- 同一浏览器发布前物品列表25个API请求（其中23个全量分页），发布后3个（数据集、第一页、一次筛选选项）；点击《1000 年后》仅1个详情请求并显示弹窗。
- 新版HTTP计时，各5次：物品页面中位67ms、范围59–1025ms；筛选接口中位200ms、范围86–497ms；详情接口中位77ms、范围67–78ms；文森特查询中位100ms、范围97–140ms。这是HTTP采样，不是完整页面渲染时延或速度承诺。
- 未触发回滚。备份时短暂停止及部署时容器替换均仅影响本站；没有变更同机其他网站。

## 保留限制

用户豁免的原生文件导入导出、确认弹窗、剩余全状态交互检查仍未补验；手工组合村民+物品参数的隐藏筛选问题仍保留，不能宣称全状态验收通过。礼物查询是已核实个人最爱子集，不是完整喜恶表。历史50项证据不足资料没有因此补正。
