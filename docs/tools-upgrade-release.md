# 实用工具升级发布状态

2026-09-26：发布准备中。当前分支 `codex/practical-tools-upgrade`，基线 `3b8cc121`。

用户明确要求“跳过直接发布”，授权跳过尚未完成的人工交互验收。未验证范围及真实证据见 `tools-upgrade-acceptance.md`；豁免不等同验收通过。仍执行自动化测试、构建、生产备份和线上冒烟检查。

恢复顺序：

1. 真实文件导入导出、确认窗口及剩余交互验收按用户决定暂缓；不清空生产用户进度。
2. 复查最终差异和构建，记录最终提交标识；仅提交本次批准文件，排除临时库、导出进度与凭据。
3. 刷新远程分支与生产运行版本，确认腾讯云目标、项目目录、磁盘及备份；不得把历史连接记录当作当前确认。
4. 部署前记录旧镜像/提交、独立备份与回滚命令；只更新该站，不影响同服务器其他网站，不迁移数据库。
5. 推送部署后验证首页、物品百科、四工具及关键链接，记录线上响应和版本；失败恢复已记录的旧版本。

## 本次备份与回滚点

- 已重新核对腾讯云目标 `106.54.240.68`、`/opt/zilong`、服务 `stardew-guide`；仓库远端为 `tomsenbor/zilong`。
- 发布前生产代码 `3b8cc1216671280950a09e5c2f610e5df3623171`，容器镜像 `sha256:ed6b12e7e7d3ab619e08c2fbad0f4775c2ca294c66516be6548e8e05b3b2f784`。
- 发布前3333条资料、48篇攻略、10个分类；容器 healthy，磁盘可用约5GB。
- 一致性备份 `/opt/stardew-backups/release-20260926-tools-005326/`：数据库、上传卷、生产环境文件和Compose文件。备份目录700，配置文件600；压缩包目录校验通过。短暂停止本站容器完成备份后已恢复 healthy。
- 回滚镜像 `stardew-guide:rollback-20260926-tools-005326` 已保留。
- 代码回滚：将该回滚镜像重新标记为 `stardew-guide-stardew-guide:latest`，在 `/opt/zilong` 执行 `docker compose --env-file .env.production up -d --no-build stardew-guide`，随后复查健康与统计。不删除卷、不回滚其他站点、不强制覆盖Git工作区。
- 若需数据恢复，应先保留故障时的数据并确认新增写入，另行恢复一致性备份；代码回滚不会自动回滚数据。

部署结果待执行后追加，不能把备份完成当作上线完成。
