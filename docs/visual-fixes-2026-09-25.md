# 视觉问题修复（2026-09-25）

本轮仅修改本地，保留此前未提交内容；没有提交、推送、部署或修改正式数据库。

## 已处理

1. `public/js/app.js`：手机菜单和搜索互斥展开，同步 aria-expanded，打开搜索时聚焦输入框。
2. `public/js/site-view.js`：首页无数量的入口显示“进入查询”，不再拼成“查询 条”。已有数量继续使用实际数据。
3. `public/js/wiki-days.js`、`public/js/site-view.js`、`public/js/app.js`：成熟周期统一为“4 天”形式，卡片、表格、详情和筛选保持一致；特殊采收窗口、邻水条件原文保留。前端筛选合并等价数值，兼容旧 URL 的纯数字。
4. `src/features/content/router.js`：仅作物 days 条件使用归一化后的准确匹配，解决 4 误匹配 14；接口结构、数据和其他筛选不变。
5. `design-system/components.css`：手机核心专题改为紧凑两列，保留五个入口及全部摘要；481–1024px 图鉴卡片改为两列。
6. `tests/visual-regressions.test.js`、`tests/api.test.js`：加入文案、单位、选项去重、菜单切换及准确筛选的回归测试。

## 验证过程

- 新增四项前端回归测试先全部按预期失败；API 测试复现 4 匹配 14；修复后相关 22 项通过。
- 首次全量测试 694 通过、2 失败，原因是原格式约定包含空格。随后统一为原约定的“4 天”，保留原断言；相关 32 项通过。
- 本地隔离预览 3174 使用临时数据库；未操作用户原有社区中心进度。3173 老预览仍保留。
- 390px 菜单→搜索后，菜单 aria-expanded=false、搜索为 true，输入框焦点和中心点击命中均为 global-search-input；反向切换收起搜索。
- 360/390/430px 专题均为两列，无页面横向溢出，五个摘要保留。
- 作物 4 天实际查询返回大蒜、防风草、齐瓜、小白菜、小麦，未混入 14 天作物；表格单位同步。
- 768px 作物卡片实测两列；社区中心工具栏仍为两行两列，没有逐字竖排。
- 1366px 首页分类最后一项显示“进入查询”，其余显示真实计数，无页面横向溢出。
- 此报告不是全站所有资料/状态的完整视觉验收；截图在本轮工具输出中，尚未形成独立截图归档。
- 最终 `npm.cmd test -- --reporter=dot`：125 个测试文件、696 项全部通过（184.40秒）。
- `npm.cmd run build`、`git diff --check` 通过；项目没有 lint 脚本。构建提示 Browserslist 数据过期，本轮没有升级依赖。
- 桌面1366px专题仍为五列；临时浏览器尺寸覆盖已恢复。

## 尚待决定的设计冲突

AGENTS.md 的木纹导航、绿色横幅、像素插画规范，与现有 v5.1 紧凑版本的明确测试约束冲突（tests/site-view.test.js 禁止恢复 hero-articles/hero-tools，工具横幅也过滤 hero 素材）。

本轮没有删除或弱化这些既有断言，也没有把全站风格更换混入局部缺陷修复。已经向用户提出选择：保留当前紧凑风格，或明确替换为木纹像素风并同步更新设计测试。在决定前，这项不能写成已完成。

## 后续用户决定

用户随后明确要求“保持当前紧凑版本”，因此不实施木纹像素风替换，也不再将与旧木纹参考图的差异视为本次发布阻塞。上述问题记录保留作过程追溯；当前设计验收以紧凑版为准。用户进一步授权推送部署，发布状态以发布记录为准，本报告的历史本地修复状态不代表发布已执行。
