# 实用工具升级验收记录

日期：2026-09-26。分支：`codex/practical-tools-upgrade`，基线：`3b8cc121`。

## 当前结论

本地实现与自动化检查已完成；发布验收未全部完成。2026-09-26 用户明确要求“跳过直接发布”，授权豁免下述未完成的人工交互验收，进入发布，不把豁免记录为通过。原有物品百科性能改动已保留。测试使用隔离临时库与 `127.0.0.1:3176`，没有修改生产数据库或正式社区中心进度。实际发布结果见 `tools-upgrade-release.md`。

## 实际实现

- 物品百科筛选选项独立接口与短期缓存；列表进入详情保留列表，只获取详情。
- 作物计算请求序号保护，防止旧响应覆盖新结果。
- 鱼类与社区中心失败状态增加重试；社区中心过期响应与存储监听不再操作新页面。
- 图鉴与鱼类、作物、礼物工具双向准确链接；匹配不唯一时回退搜索，不猜测条目。
- 新增生日与最爱礼物工具：34 位村民、138 种具体物品、197 条个人最爱关系。来源为原版 1.6.15 数据，详见 `tools-gift-evidence.md`。
- 礼物工具只展示已核实个人最爱；通用最爱、类别扩展和饰品不在本期覆盖，不宣称完整喜恶表。
- 工具首页四卡片，沿用紧凑版；768px 社区中心资料链接独立成行，避免挤成竖排。

## 自动化证据

- 用户豁免后发布前再次完整执行相同命令：130文件、720项通过，459.62秒；发布结果与公网检查见 `tools-upgrade-release.md`。

- `npm.cmd test -- --reporter=dot --maxWorkers=1`：130 个文件、720 项测试全部通过，605.39 秒。
- 最终日志：本地 `.superpowers/sdd/2026-09-25-tools-practical-upgrade/full-test-final.log`。
- 请求竞态、重试和过期社区中心响应均先复现失败，再通过最小修复变绿。
- 独立最终代码审查发现的重要问题（社区中心过期成功响应）已修复，新增回归断言验证无陈旧控件绑定。

## 浏览器检查范围

以下九个页面/状态在 360、390、768、1024、1440px 宽度均查看了截图：工具首页、礼物查询、鬼鱼查询、作物计算、社区中心、鬼鱼详情、葡萄详情、文森特详情、《1000 年后》详情。

这是本次改动相关页面的布局检查，不代表全站每个状态均已完成视觉验收。截图部分在图片懒加载结束前取得，不能单凭这些截图声明所有资源均加载成功。手机检查是浏览器视口模拟，不是实机。

已实际验证：

- 文森特筛选后分享地址与刷新保留选择，显示春10日及本期五种具体物品最爱。
- 紫水晶与春季组合返回艾米丽；查询方式切换清除不兼容选项。
- 鱼类请求阻断后出现错误，恢复网络后重试返回71条结果。
- 鬼鱼查询返回一条，显示次日02:00与45g；资料入口可打开详情。
- 葡萄、文森特详情分别提供携带精确参数的收益计算和礼物查询链接。
- 默认作物算例：600个产量×40g−6000g成本＝18000g净利润，与页面一致。
- 社区中心勾选后刷新保留1/110；临时模拟取消重置保留进度、确认重置清零。模拟已恢复，不等同原生确认窗口验收。
- 实际导出逻辑生成 schemaVersion=1、dataVersion=2、含野山葵槽位的JSON Blob。
- 768px 资料链接由宽25.77px/高105.59px调整为宽64px/高44px，截图确认不再竖排。
- 新鲜网络记录：物品列表3个API请求（数据集、第一页20条、一次filter-options）；点击《1000 年后》只增加1个详情API请求。该结果不是生产网络延迟保证。

## 尚未通过的发布门槛

1. 内置浏览器原生文件选择器与下载事件超时，无法完成“导出文件→重置→从文件选择器导入→刷新”的真实闭环。不得以模拟文件、Blob检查替代。
2. 原生确认窗口的取消/确认自动控制不可用；已完成逻辑模拟，但仍需真实交互验收。
3. 礼物空结果/失败状态、完整键盘与前进后退流程、全部详情标签及长列表底部的最终交互矩阵仍需收尾，不能标记全状态通过。

## 审查裁定与代价

- 重要问题已修复：旧社区中心请求不能绑定新页面控件。
- 次要问题暂缓：手工组合 `villager` 与 `item` 参数时API按AND筛选，但界面仅展示一种查询方式；正常界面生成单一方式。代价是这类手工链接可能有隐藏条件，需后续统一参数展示/规范化。
- 详情响应增加toolLinks而非额外请求，保留现有接口字段，避免性能回退。
- CSS复用现有组件文件，不引入新主题；不重做全站UI。
- 原50项证据不足资料仍不处理，不借本次发布声称其已补正。
- 用户明确选择当前目录新分支；保留原改动，待门槛通过再进行一次统一发布提交。
