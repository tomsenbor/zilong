# 2026-09-25 发布前视觉复核

## 范围与结论

只做本地验收，不提交、不推送、不部署。50项证据不足记录按用户决定停止补查，不由本次检查改成已核实。

社区中心取消重置、导出、导入恢复及刷新持久化已由用户截图、实际JSON与明确反馈确认，详见`wiki-release-scope-decision.md`。

## 本次浏览器实测

- 在Chrome独立测试标签1307146279打开隔离来源`http://127.0.0.1:3173/tools/community-center`。
- 请求390×844后，实际DOM为520×1125，根元素和body宽500；这不是390像素验收，不能作为该尺寸通过证据。
- 截图两次均出现`Timed out after 5000ms waiting for CDP command Page.captureScreenshot`。尝试Ctrl+0恢复正常缩放后尺寸仍不匹配。
- 已撤销临时设备尺寸设置并关闭本轮新建测试标签，不关闭用户手动验收的标签，不清除用户进度。
- 控制台有`Could not establish connection. Receiving end does not exist.`，同时出现在页面归属与多个扩展脚本归属日志中。未证明来自站点业务代码，也不能宣称控制台无错。

| 目标视口 | 本次状态 |
| --- | --- |
| 360×800 | 未完成，未获得有效尺寸截图 |
| 390×844 | 未通过验收工具校验，实际尺寸不符且截图超时 |
| 430×932 | 未完成，未获得有效尺寸截图 |
| 768×1024 | 未完成，未获得有效尺寸截图 |
| 1366×768 | 未完成，未获得有效尺寸截图 |

用户之前的桌面截图只能证明截图所示状态，不替代五尺寸完整长页、键盘、遮挡与交互验收。

## 发布记录复核

## 自动化结果

- `npm.cmd run check`：退出1；124文件、691项，123文件通过、690项通过，唯一失败为`tests/wiki-full-manifest.test.js:28`旧全名册完成声明。没有跳过或改弱该断言。
- `npm.cmd run build`：通过；构建前后Tailwind SHA-256均为`3AC1EEF7F4AF13CACA32691E81F5225817EE440020D4ABA0DBD5BA73F0346193`。caniuse-lite过期提示仅记录，未升级依赖。
- check因测试失败未进入后续步骤，因此另行运行其11个node语法检查，全部通过。
- `git diff --check`、`git diff --cached --check`通过，暂存区为空；没有lint脚本，不宣称lint通过。
- 本轮只更新验收记录，没有修改业务代码、CSS、数据库或生产配置。实际检查未达到完整视觉通过条件，当前不能签核发布。

## Git及范围

- HEAD与本地origin/main均为`044e463eba28b954be1dcce19d85875b70685c6c`；没有向远程拉取，不能把本地引用称为实时GitHub核查。
- 实用清单仍有content=pending和部分membership=pending；后续增量审计与正文对应尚未形成最终合并签核。本次不将历史pending自动置绿，也不重新开启用户叫停的50项补查。
- 最终发布必须区分旧全名册完整性声明与本次实用范围验收；旧测试不删除、不跳过。当前不能声称完整视觉验收或统一发布条件已满足。

## 后续修订：旧声明取消、视觉验收通路恢复

用户后续明确授权取消旧全名册完成声明。该决定取代上文“旧测试不改”的限制，历史测试失败记录仍保留。测试改为保留已核定集合数量、独立身份、来源与条目映射检查，不再要求全部内部集合完成；未核实资料没有被改成完成。

Chrome 的 `Page.getLayoutMetrics` 明确显示站点 zoom=0.75，解释了请求390宽实际520宽的偏差；截图命令延长到12秒仍超时。因此没有修改网站CSS来补偿验收工具误差，也没有将Chrome连接标为修复。

改用内置浏览器的正式 viewport 能力后，社区中心分别取得360×800、390×844、430×932、768×1024、1366×768准确DOM视口及实际截图。根元素scrollWidth分别为345、375、415、753、1351，无页面级横向溢出。控制台error记录为空。测试结束已 reset 临时视口（恢复1280×720），未更改用户Chrome标签或进度。

视觉工具阻塞解除不等于全站验收完成：这些是社区中心首屏截图，尚未覆盖全站长页和全部交互。768宽下工具栏四列过挤，筛选文字出现逐字换行，需作为明确布局问题后续局部处理。现有页面视觉仍需对照项目设计规范复核，不因截图成功而自动判定合格。

本轮 `npm.cmd run build` 通过；旧名册专项4项通过。`npm.cmd run check` 于19:30:53开始，207.79秒测试结束，124文件/691项全部通过，随后CSS构建与11项语法检查完成，命令退出0。`git diff --check`通过，暂存区为空；构建后CSS哈希未变。本轮未提交、推送或部署。
